participant_name = "tinkuxd"
location         = "eastus"

# admin_password is intentionally NOT set here — supply it via environment variable:
#   set TF_VAR_admin_password=<your-password>
# This keeps the password out of source control.
